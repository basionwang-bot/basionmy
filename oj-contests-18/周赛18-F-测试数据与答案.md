# F - 星图索引 · 测试数据与答案

---

## 测试点 1（样例）

**输入**
```
5
5 3 7 1 4
2
3
6
```
**预期输出**
```
YES
NO
1 3 4 5 7
```

---

## 测试点 2（重复插入）

**输入**
```
5
3 3 3 3 3
1
3
```
**预期输出**
```
YES
3
```
**说明**：重复值不重复插入，BST 只有一个节点 3，中序输出 3。

---

## 测试点 3（有序插入，退化为链）

**输入**
```
4
1 2 3 4
2
1
5
```
**预期输出**
```
YES
NO
1 2 3 4
```

---

## 测试点 4（逆序插入）

**输入**
```
4
4 3 2 1
1
2
```
**预期输出**
```
YES
1 2 3 4
```

---

## 答案（C++ 参考代码）

```cpp
#include <iostream>
#include <vector>
using namespace std;

struct Node {
    int val;
    Node *left, *right;
    Node(int v) : val(v), left(nullptr), right(nullptr) {}
};

Node* insert(Node* root, int val) {
    if (!root) return new Node(val);
    if (val < root->val) root->left  = insert(root->left,  val);
    else if (val > root->val) root->right = insert(root->right, val);
    return root;
}

bool search(Node* root, int val) {
    if (!root) return false;
    if (val == root->val) return true;
    if (val < root->val) return search(root->left,  val);
    return search(root->right, val);
}

vector<int> order;
void inorder(Node* root) {
    if (!root) return;
    inorder(root->left);
    order.push_back(root->val);
    inorder(root->right);
}

int main() {
    int n; cin >> n;
    Node* root = nullptr;
    for (int i = 0; i < n; i++) {
        int x; cin >> x;
        root = insert(root, x);
    }
    int m; cin >> m;
    while (m--) {
        int x; cin >> x;
        cout << (search(root, x) ? "YES" : "NO") << "\n";
    }
    inorder(root);
    for (int i = 0; i < (int)order.size(); i++) {
        if (i > 0) cout << " ";
        cout << order[i];
    }
    cout << "\n";
    return 0;
}
```

**关键点**：
1. BST 插入：小于根→插左子树，大于根→插右子树，等于不插；
2. BST 查找：等于即找到，否则递归左或右；
3. 中序遍历（左-根-右）输出的恰好是升序序列，验证 BST 正确性。
