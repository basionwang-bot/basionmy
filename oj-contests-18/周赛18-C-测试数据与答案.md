# C - 宇航员选拔 · 测试数据与答案

---

## 测试点 1（样例）

**输入**
```
5
Alice 30 95
Bob 50 92
Carol 25 92
Dave 20 98
Eve 35 88
```
**预期输出**
```
Alice
Carol
Bob
```

---

## 测试点 2（无人入选）

**输入**
```
3
Tom 24 99
Jerry 46 99
Mike 30 89
```
**预期输出**
```
none
```
**说明**：Tom 年龄 24<25，Jerry 年龄 46>45，Mike 评分 89<90。

---

## 测试点 3（全部入选，评分全同）

**输入**
```
3
Zoe 30 95
Amy 30 95
Ben 28 95
```
**预期输出**
```
Amy
Ben
Zoe
```
**说明**：评分全同，年龄全同（30,30,28），按年龄升序：Ben(28)先，Amy和Zoe年龄相同按姓名升序。

---

## 答案（C++ 参考代码）

```cpp
#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
using namespace std;
struct Candidate { string name; int age, score; };
bool cmp(const Candidate& a, const Candidate& b) {
    if (a.score != b.score) return a.score > b.score;
    if (a.age   != b.age)   return a.age   < b.age;
    return a.name < b.name;
}
int main() {
    int n; cin >> n;
    vector<Candidate> res;
    for (int i = 0; i < n; i++) {
        Candidate c; cin >> c.name >> c.age >> c.score;
        if (c.age >= 25 && c.age <= 45 && c.score >= 90) res.push_back(c);
    }
    if (res.empty()) cout << "none\n";
    else { sort(res.begin(), res.end(), cmp); for (auto& c : res) cout << c.name << "\n"; }
    return 0;
}
```
