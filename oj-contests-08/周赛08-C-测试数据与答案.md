# C - 仓库转运 · 测试数据与答案

---

## 测试点 1（n=2，样例）

**输入**
```
2
```
**预期输出**
```
A->B
A->C
B->C
3
```

---

## 测试点 2（n=1，最简单情况）

**输入**
```
1
```
**预期输出**
```
A->C
1
```

---

## 测试点 3（n=3，7步）

**输入**
```
3
```
**预期输出**
```
A->C
A->B
C->B
A->C
B->A
B->C
A->C
7
```

---

## 测试点 4（n=4，15步，只验证总次数）

**输入**
```
4
```
**预期输出**（最后一行）
```
15
```

（前 15 行为具体移动步骤，此处省略。）

---

## 测试点 5（n=10，验证总次数）

**输入**
```
10
```
**预期最后一行**
```
1023
```

（$2^{10} - 1 = 1023$）

---

## 答案（C++ 参考代码）

```cpp
#include <iostream>
using namespace std;

int total = 0;

void hanoi(int n, char from, char to, char via) {
    if (n == 1) {
        cout << from << "->" << to << endl;
        total++;
        return;
    }
    hanoi(n - 1, from, via, to); // 把 n-1 个从 from 移到 via（借助 to）
    cout << from << "->" << to << endl;
    total++;
    hanoi(n - 1, via, to, from); // 把 n-1 个从 via 移到 to（借助 from）
}

int main() {
    int n;
    cin >> n;
    hanoi(n, 'A', 'C', 'B');
    cout << total << endl;
    return 0;
}
```

### 递归调用树（n=3 示意）

```
hanoi(3, A, C, B)
├── hanoi(2, A, B, C)
│   ├── hanoi(1, A, C, B) → 输出 A->C
│   ├── 输出 A->B
│   └── hanoi(1, C, B, A) → 输出 C->B
├── 输出 A->C
└── hanoi(2, B, C, A)
    ├── hanoi(1, B, A, C) → 输出 B->A
    ├── 输出 B->C
    └── hanoi(1, A, C, B) → 输出 A->C
```

共输出 7 行 + 最后总次数 = 8 行。
